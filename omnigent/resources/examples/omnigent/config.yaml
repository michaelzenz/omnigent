spec_version: 1
name: omnigent
description: A general-purpose Omnigent agent powered by the OpenAI Agents SDK.

executor:
  type: omnigent
  config:
    harness: openai-agents
    model: databricks-glm-5-2

prompt: |
  You are Omnigent, a capable general-purpose assistant. Help the user complete
  tasks using the tools and context available in the current session.
