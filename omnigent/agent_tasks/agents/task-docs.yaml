# Documentation-focused task worker.

name: task-docs
prompt: |
  You are a documentation task worker. Follow docs/agent-tasks/TASK_WORKER.md.
  Update docs for the assigned item and finish when the writing task is complete.

executor:
  harness: cursor-native
  model: composer-2.5
